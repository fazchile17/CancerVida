rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Por ahora, no se usa Firestore
    // Estas reglas pueden expandirse si se necesita almacenar datos
    match /{document=**} {
      allow read, write: if false;
    }
  }
}

